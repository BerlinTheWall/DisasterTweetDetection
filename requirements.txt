# Core pipeline: preprocessing + the TF-IDF baseline.
# Python 3.9+.  Install with:  pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
nltk>=3.9
pyspellchecker>=0.8        # replaces `autocorrect`, which no longer builds
pytest>=7.4                # tests only

# Notebooks / plots
matplotlib>=3.7
seaborn>=0.13
jupyter>=1.0

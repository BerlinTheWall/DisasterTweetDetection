# Install PyTorch FIRST, matched to your CUDA version:
#   https://pytorch.org/get-started/locally/
# e.g.  pip install torch --index-url https://download.pytorch.org/whl/cu124
# Then:  pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

transformers>=4.40        # train.py
sentencepiece>=0.2        # DeBERTa-v3 tokenizer
protobuf>=4.25            # DeBERTa-v3 tokenizer conversion

pytest>=7.4               # tests only
